services:
  - type: web
    name: NajjarStaffWallet-Node
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.16.11
      - key: DATABASE_URL
        sync: false
